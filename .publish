#!/bin/bash

echo "Publishing nuget packages"
dotnet nuget push "$2/*.nupkg" --no-symbols --api-key $NUGET_TOKEN --source "github"

echo "Publishing docker image"
docker push $DOCKER_REGISTRY_URL/golden-reports:latest